name: CI

on:
  push:
    branches:
      - main
      - 'modernization/**'
  pull_request:
    branches:
      - main

permissions:
  contents: write

jobs:
  quality:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      NODE_ENV: test
      PORT: 3000
      DATABASE_URL: postgresql://fintech:fintech_test@localhost:5432/fintech_test?schema=public
      JWT_ACCESS_SECRET: ci-only-secret-that-is-at-least-thirty-two-characters-long
      JWT_ACCESS_TTL_SECONDS: 900
      CORS_ORIGIN: http://localhost:3000
      LOG_LEVEL: info

    services:
      postgres:
        image: postgres:17-alpine
        env:
          POSTGRES_DB: fintech_test
          POSTGRES_USER: fintech
          POSTGRES_PASSWORD: fintech_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U fintech -d fintech_test"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Use Node.js 22
        uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies and synchronize lockfile
        run: npm install --no-fund

      - name: Commit dependency lockfile
        if: github.ref == 'refs/heads/modernization/phase-2-identity-wallets' && github.event_name == 'push'
        run: |
          if git diff --quiet -- package-lock.json; then
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add package-lock.json
          git commit -m 'chore: sync phase two dependency lockfile'
          git push origin HEAD:modernization/phase-2-identity-wallets

      - name: Audit production dependencies
        run: npm audit --omit=dev --audit-level=high

      - name: Generate Prisma client
        run: npm run prisma:generate

      - name: Quality gates
        run: npm run ci

      - name: Validate container definitions
        run: |
          docker compose config --quiet
          docker build --tag fintech-transaction-platform:ci .

      - name: Smoke test PostgreSQL readiness endpoint
        shell: bash
        run: |
          test -f dist/main.js
          node dist/main.js > /tmp/fintech-api.log 2>&1 &
          APP_PID=$!

          cleanup() {
            if kill -0 "$APP_PID" 2>/dev/null; then
              kill "$APP_PID"
            fi
          }
          trap cleanup EXIT

          for attempt in {1..20}; do
            if curl --fail --silent http://localhost:3000/api/v1/health/ready > /tmp/health.json; then
              cat /tmp/health.json
              exit 0
            fi

            if ! kill -0 "$APP_PID" 2>/dev/null; then
              echo 'Application exited before readiness check succeeded.'
              cat /tmp/fintech-api.log
              exit 1
            fi

            sleep 1
          done

          echo 'Readiness endpoint did not become healthy in time.'
          cat /tmp/fintech-api.log
          exit 1
